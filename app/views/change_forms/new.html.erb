<%= form_with(url: change_forms_create_path, local: true) do |form| %>
  <% if @hr_form.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@hr_form.errors.count, "error") %> prohibited this change_form from being saved:</h2>

      <ul>
      <% @hr_form.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
<h1>ChangeForms#new</h1>
<p>Find me in app/views/change_forms/new.html.erb</p>
